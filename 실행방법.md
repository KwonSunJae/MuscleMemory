# Muscle Memory 실행 가이드

## 1. 설치 방법
```bash
# 1. 저장소 클론
git clone https://github.com/your-repo/musclememory.git

# 2. 가상환경 생성 및 활성화
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 3. 패키지 설치
pip install -e .
```

## 2. 기본 사용법
### 2.1 프로젝트 초기화
```bash
# 새 프로젝트 생성
muscle init <repository_url> <project_name> <config_type>

# 예시
muscle init https://github.com/user/infra-repo test terraform
```

### 2.2 작업 시작
```bash
# 프로젝트 작업 시작
muscle ready <project_name>

# 예시
muscle ready test
```

### 2.3 파일 추가 및 커밋
```bash
# 파일 추가
muscle add <project_name> <file_pattern>

# 예시
muscle add test main.tf          # 단일 파일
muscle add test *.tf             # 모든 terraform 파일
muscle add test terraform/*.tf   # 특정 디렉토리의 terraform 파일
```

### 2.4 변경사항 등록
```bash
# 변경사항 커밋 및 푸시
muscle enroll <project_name>

# 예시
muscle enroll test
```

### 2.5 작업 완료
```bash
# 작업 종료 및 잠금 해제
muscle complete <project_name>

# 예시
muscle complete test
```

### 2.6 IaC 파일 관리
```bash
# 최신 IaC 파일 불러오기
muscle fetch latest <project_name>

# 예시
muscle fetch latest test

# 특정 버전의 IaC 파일 불러오기
muscle fetch version <project_name> <version>

# 예시
muscle fetch version test abc123    # commit hash로 불러오기
muscle fetch version test v1.0.0    # 태그로 불러오기

# 버전 히스토리 조회
muscle history <project_name> [--limit N] [--format simple|detailed]

# 예시
muscle history test --limit 5
muscle history test --format detailed

# 특정 버전으로 롤백
muscle rollback <project_name> <version> [--force] [--backup]

# 예시
muscle rollback test abc123         # 기본 롤백
muscle rollback test v1.0.0 --force # 강제 롤백
muscle rollback test abc123 --backup # 현재 상태 백업 후 롤백
```

## 3. 주의사항
- 반드시 `ready` 명령어로 시작하여 `complete` 명령어로 종료
- 한 프로젝트는 동시에 한 사용자만 작업 가능
- 기본 타임아웃은 5분 (300초)
- 작업 중 비정상 종료 시 자동으로 타임아웃 후 잠금 해제

## 4. 설정 파일
### 4.1 전역 설정 (~/.muscle/config.json)
```json
{
    "repository_url": "https://github.com/user/infra-repo",
    "current_project": "test"
}
```

### 4.2 프로젝트 설정 (project/config)
```
project_type=terraform
project_name=test
project_repo=https://github.com/user/infra-repo
```

## 5. 오류 해결
### 5.1 잠금 파일 관련
```bash
# 강제로 잠금 해제 (관리자 전용)
rm ~/.muscle/workspace/test/test/.lock

# 타임아웃 대기
# 기본 5분 후 자동 해제
```

### 5.2 Git 관련
```bash
# 저장소 재설정
cd ~/.muscle/workspace/test
git remote set-url origin <new_url>

# 브랜치 초기화
git checkout main
git reset --hard origin/main
```