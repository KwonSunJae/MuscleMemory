# Muscle Memory 프로젝트 상세 설명

## 1. 프로젝트 배경 및 목적
- **문제 인식**: 여러 개발자가 동시에 인프라 작업 시 발생하는 충돌 문제
- **해결 방안**: Git 기반의 작업 관리 + MAC 주소 기반의 잠금 시스템
- **최종 목표**: 안전하고 체계적인 인프라 작업 관리 시스템 구축

## 2. 핵심 기능 상세
### 2.1 프로젝트 초기화 (init)
```bash
muscle init <repo_url> <project_name> <config_type>
```
- **브랜치 구조**:
  - main: muscle.init 파일 (MAC 주소, 타임아웃 설정)
  - blank: 빈 브랜치 (템플릿용)
  - project: 실제 작업 브랜치 (config 파일 포함)

### 2.2 작업 시작 (ready)
```bash
muscle ready <project_name>
```
- MAC 주소 암호화하여 .lock 파일 생성
- 기본 타임아웃 5분 설정
- 다른 사용자의 접근 차단

### 2.3 작업 완료 (complete)
```bash
muscle complete <project_name>
```
- .lock 파일 제거
- 작업 상태 초기화

### 2.4 파일 관리
```bash
muscle add <project_name> <file_pattern>
muscle enroll <project_name>
```
- add: Git 스테이징 영역에 파일 추가
- enroll: 변경사항 커밋 및 원격 저장소 푸시

### 2.5 IaC 파일 관리
```bash
muscle fetch latest|version <project_name> [<version>]
muscle history <project_name>
muscle rollback <project_name> <version>
```
- fetch: IaC 파일 불러오기 (최신/특정 버전)
- history: 버전 히스토리 조회
- rollback: 특정 버전으로 롤백

## 3. 프로젝트 구조
```
musclememory/
├── commands/
│   ├── init.py
│   ├── ready.py
│   ├── complete.py
│   ├── add.py
│   ├── enroll.py
│   ├── fetch.py
│   ├── history.py
│   ├── rollback.py
│   └── config.py
├── models/
│   ├── lock.py
│   └── project.py
├── utils/
│   ├── system.py
│   ├── config.py
│   └── crypto.py
└── cli.py
```

## 4. 현재까지의 개발 진행 상황
### 4.1 완료된 작업
- 기본 명령어 시스템 구현
- Git 작업 관리 기능 구현
- MAC 주소 기반 잠금 시스템 구현
- 설정 파일 관리 시스템 구현
- IaC 파일 관리 기능 구현
  - 파일 불러오기
  - 버전 히스토리 관리
  - 롤백 시스템

### 4.2 현재 진행 중인 작업
- 테스트 코드 작성
  - 유틸리티 테스트
  - 모델 테스트
  - 명령어 테스트
  - 통합 테스트

### 4.3 코드 관리 현황
- 이슈 기반 개발 진행
- 커밋 컨벤션 적용
  - feat: 새로운 기능
  - fix: 버그 수정
  - test: 테스트 코드
  - chore: 기타 변경사항

## 5. 당면 과제
### 5.1 시급한 개선사항
- 다른 파트와의 호환성
  - Infrastructure 파트 연동
  - Security 파트 연동
  - Monitoring 파트 연동

### 5.2 예외 처리 강화
- 네트워크 오류 처리
- 동시성 문제 처리
- 권한 관련 예외 처리

### 5.3 상태 관리 개선
- 프로젝트 상태 추적
- 오류 상태 복구
- 작업 진행 상태 관리

## 6. 향후 계획
### 6.1 단기 목표
- 테스트 코드 완성
- 예외 처리 구현
- 다른 파트와의 기본 연동

### 6.2 중장기 목표
- 프로젝트 관리 체계화
  - 이슈/PR 템플릿 구축
  - 테스트 환경 구축
  - 역할 분담 체계화
- 안정성 및 성능 최적화
- 사용자 피드백 반영