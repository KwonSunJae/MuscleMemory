1. **각 명령어별 상세 동작 과정**
```python
# init 명령어 예시
muscle init https://github.com/user/repo test terraform

1. 전역 설정 저장
   - ~/.muscle/config.json에 저장소 URL 저장

2. 작업 공간 생성
   - ~/.muscle/workspace/test/ 디렉토리 생성

3. Git 저장소 초기화
   - git init
   - remote 추가
   - main 브랜치 생성/체크아웃
   
4. muscle.init 파일 생성
   - timeout=300
   - mac_address=<encrypted_mac>

5. 브랜치 생성
   - blank 브랜치 생성
   - test 브랜치 생성 및 config 파일 추가

6. **IaC 파일 관리 시스템 상세 동작**
```python
# fetch 명령어 실행 시
1. 최신 버전 불러오기
   - 프로젝트 브랜치 체크아웃
   - 원격 저장소에서 최신 상태 가져오기
   - 로컬 변경사항 검증

2. 특정 버전 불러오기
   - 프로젝트 브랜치 체크아웃
   - 지정된 버전으로 체크아웃
   - 버전 유효성 검증

# history 명령어 실행 시
1. 버전 히스토리 조회
   - 커밋 히스토리 가져오기
   - 상세 정보 포맷팅 (작성자, 날짜, 메시지)
   - 변경된 파일 목록 표시 (상세 모드)

# rollback 명령어 실행 시
1. 롤백 준비
   - 현재 상태 검증
   - 백업 브랜치 생성 (옵션)
   - 대상 버전 유효성 검증

2. 롤백 수행
   - 지정된 버전으로 리셋
   - 원격 저장소 강제 푸시
   - 실패 시 백업에서 복구
```
```

2. **Lock 시스템 상세 동작**
```python
# ready 명령어 실행 시
1. MAC 주소 획득 및 암호화
   - getnode()로 MAC 주소 획득
   - Fernet 암호화 적용

2. Lock 파일 생성
   {
     "owner": "<encrypted_mac>",
     "expired_time": "<current_time + 300s>",
     "project": "test",
     "status": "working"
   }

3. 상태 검증
   - 기존 Lock 파일 존재 여부 확인
   - 만료 시간 검증
   - 소유자 확인
```

3. **예외 처리 시나리오**
```python
# 주요 예외 상황 및 처리
1. Git 작업 실패
   - 원격 저장소 연결 실패
   - 브랜치 충돌
   - 푸시 권한 없음

2. Lock 관련 예외
   - 잠금 파일 충돌
   - 만료된 잠금
   - 권한 없는 접근

3. 설정 관련 예외
   - 설정 파일 누락
   - 잘못된 설정값
   - 작업 공간 접근 불가
```

4. **다른 파트와의 연동 계획**
```python
# Infrastructure 파트 연동
1. 프로젝트 생성 시
   - 인프라 템플릿 자동 생성
   - 리소스 상태 확인 API 연동

# Security 파트 연동
1. 사용자 인증
   - 토큰 기반 인증 추가
   - 권한 레벨 확인

# Monitoring 파트 연동
1. 작업 상태 모니터링
   - 작업 시작/종료 이벤트 전송
   - 리소스 변경 알림
```